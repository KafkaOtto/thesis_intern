apiVersion: v1
kind: Service
metadata:
  name: {{ include "bm25s.fullname" . }}
  labels:
    {{- include "bm25s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bm25s.selectorLabels" . | nindent 4 }}
