apiVersion: apps/v1
kind: Deployment
metadata:
  name: DeepSeek-R1-Distill-Llama-8B
  namespace: default
  labels:
    app: DeepSeek-R1-Distill-Llama-8B
spec:
  replicas: 1
  selector:
    matchLabels:
      app: DeepSeek-R1-Distill-Llama-8B
  template:
    metadata:
      labels:
        app: DeepSeek-R1-Distill-Llama-8B
    spec:
      volumes:
      - name: cache-volume
        persistentVolumeClaim:
          claimName: DeepSeek-R1-Distill-Llama-8B
      # vLLM needs to access the host's shared memory for tensor parallel inference.
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: "2Gi"
      containers:
      - name: DeepSeek-R1-Distill-Llama-8B
        image: vllm/vllm-openai:v0.7.3
        command: ["/bin/sh", "-c"]
        args: [
          "vllm serve deepseek-ai/DeepSeek-R1-Distill-Llama-8B --trust-remote-code --port 8002"
        ]
        ports:
        - containerPort: 8002
        resources:
          limits:
            cpu: "1"
            memory: 20G
            nvidia.com/gpu: "1"
          requests:
            cpu: "1"
            memory: 16G
            nvidia.com/gpu: "1"
        volumeMounts:
        - mountPath: /root/.cache/huggingface
          name: cache-volume
        - name: shm
          mountPath: /dev/shm
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 5