apiVersion: apps/v1
kind: Deployment
metadata:
  name: deepseek-r1-distill-llama-8b
  namespace: default
  labels:
    app: deepseek-r1-distill-llama-8b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deepseek-r1-distill-llama-8b
  template:
    metadata:
      labels:
        app: deepseek-r1-distill-llama-8b
    spec:
      volumes:
      - name: cache-volume
        persistentVolumeClaim:
          claimName: deepseek-r1-distill-llama-8b
      # vLLM needs to access the host's shared memory for tensor parallel inference.
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: "2Gi"
      containers:
      - name: deepseek-r1-distill-llama-8b
        image: vllm/vllm-openai:v0.7.3
        command: ["/bin/sh", "-c"]
        args: [
          "vllm serve deepseek-ai/DeepSeek-R1-Distill-Llama-8B --trust-remote-code --max-model-len 20000 --port 8002"
        ]
        ports:
        - containerPort: 8002
        resources:
          limits:
            cpu: "1"
            memory: 20G
            nvidia.com/gpu: "1"
          requests:
            cpu: "1"
            memory: 16G
            nvidia.com/gpu: "1"
        volumeMounts:
        - mountPath: /root/.cache/huggingface
          name: cache-volume
        - name: shm
          mountPath: /dev/shm
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 5